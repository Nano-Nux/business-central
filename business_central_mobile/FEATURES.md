# Mobile Features

| Feature | Status | Notes |
|---|---|---|
| Flutter application shell | Partial | Riverpod bootstrap, adaptive native navigation, guarded permission/module destinations, staff shop-lock presentation, and native dashboard/catalog/POS entry screens are implemented |
| Portal WebView device bridges | Implemented for current scope | Local HTTP portal deployments automatically use a native `mobile_scanner` camera view for product, stock, IMEI, and serial capture; keyboard/HID scanners use focused portal input; Bluetooth thermal discovery, connection, and ESC/POS byte transport use the native printer bridge on Android/iOS; a top-of-page downward pull reloads the current portal page with visible pull/release feedback and duplicate suppression; startup main-frame failures automatically retry so a persisted portal service worker can recover during temporary outages |
| Merchant/manager/staff authentication | Partial | Native ONLINE login/refresh/logout contract and secure token storage plus FULLY_OFFLINE owner and locally provisioned staff setup/login are implemented; manager-specific local policy and account recovery remain pending |
| Role-based navigation and permissions | Partial | Shell destinations are filtered by permissions/modules and staff are locked to their assigned shop; FULLY_OFFLINE local owners can assign existing permission definitions to local roles and enable/disable merchant-enabled modules per shop; permission-definition creation and broader policy coverage remain pending |
| Merchant dashboard | Partial | ONLINE shop-scoped summary and sales-by-day reads are implemented; FULLY_OFFLINE summary/day reads derive from local orders; richer local cost metrics remain pending |
| Catalog read surface | Partial | ONLINE category/product search reads cache successful merchant-scoped responses in SQLite, falls back to cache when transport is denied, loads product variants in a native detail sheet, and permission-gated native category/product/variant CRUD is wired to backend contracts; FULLY_OFFLINE local category/product/variant CRUD and local pricing are durable in SQLite; images and richer inventory policy remain pending |
| Measurement units and conversions | Partial | ONLINE permission-gated native unit/conversion list/create/delete uses canonical `/units` and `/unit-conversions` routes; FULLY_OFFLINE unit storage, conversion validation, and local create/delete workflows are durable; cross-domain synchronization remains pending |
| Pricing | Partial | Permission-gated native price-list and variant-price reads/create/delete/upsert use the backend pricing contract; FULLY_OFFLINE has a local merchant price-list/price layer that updates the local POS variant price; price scheduling and richer variant presentation remain pending |
| POS | Partial | ONLINE shop-scoped catalog, cart, backend-authoritative exact quote, customer capture, promotion selection, idempotent payment checkout, canonical captured-payment refunds, and exact invoice projection are implemented; FULLY_OFFLINE local catalog/cart, exact quote, atomic local order persistence, FIFO stock consumption, captured local payment records, idempotent refund records, and exact invoice projection are implemented; ONLINE temporary-offline financial mutation policy remains pending |
| Inventory location and stock-in surface | Partial | ONLINE selected-shop locations are displayed with merchant/shop-scoped SQLite fallback, permission-gated stock receiving sends backend-idempotent event keys, and selected-shop movement history/detail reads resolve product/location/document/FIFO context; FULLY_OFFLINE local receiving with purchase-order/receipt/batch/expiry context, POS and repair-part stock-out, exact FIFO layer allocation, balance updates, and movement detail are implemented; transfer/adjustment parity and ONLINE offline mutation remain pending |
| Financial reports | Partial | ONLINE selected-shop summary, daily sales, and top-product reports are implemented with native range selection; FULLY_OFFLINE local order summary/day/top-product reads include FIFO COGS, refund totals, refund-adjusted gross profit, and margin for tracked stock |
| Transaction history | Partial | ONLINE selected-shop event list, filters, and detail sheet expose canonical order lines, FIFO cost/profit, payments, and refunds; FULLY_OFFLINE local order, inventory movement, POS refund, and repair checkout/refund events/details include FIFO cost/profit, receipt context, local captured-payment projections, and exact invoice/thermal print actions; backend reconciliation remains pending |
| Delivery management | Partial | ONLINE selected-shop delivery list/create/remove and POS delivery selection are implemented; FULLY_OFFLINE delivery options and selected delivery IDs are durable in SQLite; temporary-offline synchronization and delivery fee policy remain pending |
| Invoice read surface | Partial | ONLINE selected-shop invoice list and native detail sheet are implemented; FULLY_OFFLINE invoice projections read local orders/lines plus repair service/part records, including every repair issue and non-empty condition; native PDF generation/share/OS print and local image-first ESC/POS thermal output preserve exact sales and repair invoice totals; canonical numbering and reconciliation remain pending |
| Customer history | Partial | ONLINE selected-shop view-only projection combines canonical invoice and repair-order names/phones; FULLY_OFFLINE derives a view-only sales/repair projection from local orders and repair records; customer CRUD remains disabled |
| Staff account management | Partial | ONLINE native membership list, add, activate/deactivate, and delete controls use the canonical `/users` contract and `membership.manage`; FULLY_OFFLINE staff provisioning, Argon2id credentials, shop assignment, active-state changes, local role-permission editing, shop-module controls, and append-only audit events use the scoped tenant graph; permission-definition creation and reconciliation remain pending |
| Repair intake and list | Implemented for current scope | ONLINE selected-shop repair list and atomic canonical ticket intake preserve stable repeatable work-item IDs plus ordered repeatable issues and optional conditions; native diagnostics, payments, scoped/idempotent refunds, parts, images, approvals, warranties, lifecycle status transitions, repeatable work-item reads, and invoice reads use backend contracts. FULLY_OFFLINE stores the same child records and issue/condition arrays plus repeatable work-item/device/field projections in the merchant/shop-scoped local repair ledger, validates child work-item scope, consumes local FIFO stock for used catalog parts, and recalculates repair totals; reconnect reconciliation supports the aggregate and dependency-ordered child creates, with ticket-level billing authoritative. Canonical shop-level preset administration is currently provided by the portal Repairs subpages; native preset administration remains a parity follow-up. |
| Shop settings | Partial | ONLINE selected-shop profile, timezone, tax, and receipt/footer note reads and updates are implemented for `membership.manage`; FULLY_OFFLINE persists the same selected-shop profile/tax/receipt fields locally; device-local Bluetooth thermal profiles, paper width, font scale, WebView native printer transport, and local shop-scoped repair fault presets/default duration are implemented; repair specifications remain local-only because the portal has no backend contract |
| Promotions | Partial | ONLINE promotion definition list/create/delete supports schedules and usage limits, and native detail controls manage promotion codes and product/variant scopes; FULLY_OFFLINE provides the same local catalog/scope/code workflow with schedule, minimum, usage-limit, exact POS discount/tax validation, and product/variant-scoped repair-part application; standalone local service billing rejects promotion application until a canonical order relationship exists, and ONLINE temporary-offline synchronization remains pending |
| Service catalog and orders | Partial | ONLINE and FULLY_OFFLINE services-module catalog/order/item/appointment/note/billing reads and writes are implemented through merchant/shop-scoped repositories; native catalog/order edit/delete controls are available, local billing is explicitly standalone and not external payment capture; child-record edit/delete detail controls and backend reconciliation remain pending |
| Online runtime mode | Partial | Fail-closed configuration, connectivity hard-stop, authenticated API transport, token rotation, workspace hydration, and guarded navigation are implemented |
| Temporary offline operation in ONLINE mode | Partial | Merchant/workspace and catalog reads have scoped SQLite cache fallback; selected-shop settings and repeatable repair-ticket intake plus dependency-ordered repair child creates are transactionally persisted/queued with payload hashes, retry/conflict statuses, checkpointed reconciliation, stable work-item IDs, and server-authoritative results; financial, inventory, payment, approval, permission, module, promotion, and work-item update/delete mutations remain online-only or deferred pending domain policies |
| FULLY_OFFLINE runtime mode | Partial | `.env` aliases resolve to local-only mode, inject a network-denying adapter, support transactional owner/staff setup and login, and expose local merchant-scoped catalog, measurement, pricing, POS checkout/refunds, delivery options, inventory ledger, invoices, refund-adjusted reports, customers, history, staff, backup/restore, service, repair child records/refunds/used-part FIFO effects, and settings workflows; remaining catalog features remain pending |
| Local SQLite database | Partial | Drift schema version 27 contains scoped identity/contact, tenant, shop/timezone, shop-scoped settings, module, device, session, entity-version, checkpoint, operation queue, catalog, measurement unit/conversion, local delivery, location cache, local price-list/price, local POS order/line/payment/refund/promotion/order-promotion tables with exact discount/tax fields, local inventory ledger/FIFO cost-layer/allocation plus purchase-order/receipt/batch/expiry context, append-only local audit-event, device-local printer profile, payload-preserving canonical record cache with tombstones, local service-record, and local repair-record/work-item ticket, child, and field projections; typed canonical operational repositories and broader reconciliation policies remain pending |
| Local audit and recovery | Partial | FULLY_OFFLINE owner/setup, staff, inventory, POS, promotion, and repair mutations write merchant/shop/actor-scoped append-only audit records; authenticated JSON backup/restore preserves settings, promotion definitions/scopes/codes, order promotion links, and pending operation rows while excluding credentials and authorization assignments; deterministic SHA-256 checksums, password-protected Argon2id/AES-GCM backup envelopes, clipboard controls, native file save/open, platform share-sheet export, and native encrypted SQLite storage are implemented |
| Reconnection synchronization for ONLINE mode | Partial | Backend handshake/push/pull contract and mobile Riverpod worker support shop settings, repeatable repair-ticket aggregate CREATE, and dependency-ordered repair child CREATE operations with durable idempotency, payload hashes, retry/backoff, explicit rejection/conflict statuses, ordered changes, checkpoints, server-authoritative totals, stable work-item IDs, and child projections; external payments, promotions, and work-item financial allocation remain online/deferred policies |
| Service workflows | Partial | Catalog/order intake, item reads/addition, appointments, notes, billing, updates, and deletes are implemented ONLINE against backend routes and FULLY_OFFLINE against a durable local service-record store; native edit/delete detail controls and reconciliation remain pending |
| Repair workflows | Implemented for current scope | The shared repair contract accepts repeatable device work items; atomic ONLINE intake/list, backend-defined ticket/work-item field rendering, work-item reads, native repeating intake UI, diagnostics, payments, scoped/idempotent refunds, lifecycle transitions, and invoice reads remain implemented against backend contracts. FULLY_OFFLINE reads cached form definitions, persists ticket/work-item/device/field children, validates child references, exposes them through the local repository, and includes them in invoice projections without external capture; ONLINE temporary-offline ticket and supported child CREATE operations reconcile with server totals and canonical projections. |
